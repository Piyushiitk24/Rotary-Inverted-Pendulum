\usepackage[T1]{fontenc}
\usepackage{microtype}
\usepackage{mathtools}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{subcaption}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{siunitx}
\usepackage[numbers,sort&compress]{natbib}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,calc}

\IfFileExists{placeins.sty}{%
  \usepackage{placeins}%
}{%
  \providecommand{\FloatBarrier}{}%
}

\graphicspath{{figures/}{images/}}

\DeclareSIUnit{\step}{step}
\sisetup{
  detect-all,
  group-digits = integer,
  group-separator = {,},
  group-minimum-digits = 4
}

\hypersetup{
  pdftitle={\ThesisTitle},
  pdfauthor={\ThesisAuthor},
  pdfsubject={Master of Technology thesis},
  pdfkeywords={rotary inverted pendulum, Furuta pendulum, sliding mode control, state feedback, stepper motor},
  colorlinks=true,
  linkcolor=blue,
  citecolor=blue,
  urlcolor=blue
}

\setcounter{tocdepth}{2}
\setcounter{secnumdepth}{3}

\setcounter{topnumber}{2}
\setcounter{bottomnumber}{2}
\setcounter{totalnumber}{4}
\renewcommand{\topfraction}{0.90}
\renewcommand{\bottomfraction}{0.80}
\renewcommand{\textfraction}{0.08}
\renewcommand{\floatpagefraction}{0.85}

\providecommand{\tightlist}{}

\newcommand{\IncludeGraphicAuto}[2]{%
  \IfFileExists{#1.pdf}{%
    \includegraphics[width=#2]{#1.pdf}%
  }{%
    \IfFileExists{#1.png}{%
      \includegraphics[width=#2]{#1.png}%
    }{%
      \PackageError{thesis}{Required figure #1 is missing}%
        {Regenerate the thesis assets before building.}%
    }%
  }%
}

\newcommand{\ThesisFigure}[4]{%
  \begin{figure}[htbp]
    \centering
    \IncludeGraphicAuto{#1}{#4}
    \caption{#2}
    \label{#3}
  \end{figure}
}

\newcommand{\InputRequired}[1]{%
  \IfFileExists{#1}{\input{#1}}{%
    \PackageError{thesis}{Required input #1 is missing}%
      {Regenerate the thesis assets before building.}%
  }%
}

\newcommand{\ObservedBox}[1]{%
  \begin{center}
    \fbox{\parbox{0.95\linewidth}{\textbf{Experimental evidence.}~#1}}
  \end{center}
}
